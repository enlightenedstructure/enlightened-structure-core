{
  "name": "enlightenedstructure-core",
  "version": "0.0.2",
  "description": "",
  "scripts": {
    "dev": "npm run start & npm run watch",
    "publish": "coffee bin/publish",
    "start": "harp server --port ${PORT:=9000} src",
    "test": "curl -I http://www.enlightenedstructure.net/",
    "watch": "browser-sync start --port 8080 --proxy \"localhost:${PORT:=9000}\" --files 'src/**/*'"
  },
  "author": "Harlan T Wood",
  "devDependencies": {
    "browser-sync": "^1.3.7",
    "coffee-script": "^1.9.2",
    "gh-pages": "^0.3.1"
  }
}
